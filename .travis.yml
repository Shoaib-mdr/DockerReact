sudo: required

services:
    - docker

before_install:
    - docker build -t shoaib-mdr/dockerReact -f Dockerfile.dev .

scripts:
    - docker run Shoaib-mdr/DockerReact npm run test -- --coverage


